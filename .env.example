# Environment Variables Template
# Copy this to .env.local and fill in your actual values
# NEVER commit .env.local to version control

# API Proxy URL (backend service that handles API keys securely)
VITE_API_PROXY_URL=http://localhost:3001/api/proxy

# Supabase Configuration (public keys are safe to expose)
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Feature Flags
VITE_ENABLE_TRINITY=false
VITE_TRINITY_DEBUG=false

# SECURITY WARNING:
# - NEVER put actual API keys in VITE_ environment variables
# - All VITE_ variables are exposed to the browser
# - Use a secure backend proxy for sensitive API calls
# - The following variables should ONLY exist on your backend server:
#   - OPENAI_API_KEY
#   - FAL_API_KEY
#   - Any other sensitive credentials